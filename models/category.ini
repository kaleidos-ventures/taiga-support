[model]
name = Category
label = {{ this.name }}
hidden = yes

[children]
model = article
order_by = order, title

[fields.name]
label = Name
type = string

[fields.order]
label = Order
type = sort_key

[fields.body]
label = Body
type = markdown
